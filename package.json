{
  "name": "md-strip",
  "version": "1.0.0",
  "description": "Strip markdown formatting to plain text",
  "type": "module",
  "bin": {
    "md-strip": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^25.9.2",
    "typescript": "^5.8.3",
    "vitest": "^3.2.1"
  }
}
